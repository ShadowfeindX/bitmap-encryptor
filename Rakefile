#require 'rake/testtask'

#Rake::TestTask.new do |t|
#  t.libs << "tests"
#  t.test_files = FileList['tests/test*.rb']
#  t.verbose = true
#end
#$:.unshift 'lib'

task default: %w[start]

task :start do
    ruby 'bin/main.rb'
end